class Solution {
    public int findDuplicate(int[] nums) {
        int n=nums.length;
        for(int i:nums){
            int ind=Math.abs(i);
            if(nums[ind]<0) return ind;
            nums[ind]=-nums[ind];
        }
        return n;
    }
}
